{
  "name": "srilanka-hindu-temples-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker to fetch temples from MongoDB Atlas",
  "type": "module",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'No build step needed for Worker'",
    "start": "wrangler dev"
  },
  "dependencies": {
    "mongodb": "^6.9.0"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
